# Dependencias dos scripts do dataset (geracao + carga).

# Carga no MongoDB (carregar_mongo.py) — le .env/CSV com a stdlib.
pymongo>=4.6,<5
# Conexoes "mongodb+srv://" (MongoDB Atlas).
dnspython>=2.0

# Geracao dos CSVs (gerar_*.py / gerar_dados.py), usada automaticamente pelo
# loader quando os arquivos estao ausentes.
faker>=24
pandas>=2.0
