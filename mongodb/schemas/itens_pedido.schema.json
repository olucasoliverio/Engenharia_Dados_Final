{
  "$jsonSchema": {
    "bsonType": "object",
    "title": "itens_pedido",
    "required": [
      "id_item", "id_pedido", "id_produto", "quantidade",
      "valor_unitario", "desconto_percentual", "subtotal", "updated_at"
    ],
    "properties": {
      "id_item": { "bsonType": ["int", "long"], "description": "Identificador único" },
      "id_pedido": { "bsonType": ["int", "long"], "description": "Referência -> pedidos.id_pedido" },
      "id_produto": { "bsonType": ["int", "long"], "description": "Referência -> produtos.id_produto" },
      "quantidade": { "bsonType": ["int", "long"] },
      "valor_unitario": { "bsonType": ["double", "int", "long"] },
      "desconto_percentual": { "bsonType": ["double", "int", "long"] },
      "subtotal": { "bsonType": ["double", "int", "long"] },
      "updated_at": { "bsonType": "date", "description": "Controle de carga incremental" }
    }
  }
}
